let os = require("os");
console.log(os.arch()); // gives architecture 32 bits or 64 bits
console.log(os.networkInterfaces()); // gives wifi information
console.log(os.cpus()); // gives server information
